# macOS
.DS_Store

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Local deploy scripts — they contain your GitHub token. NEVER publish them.
*.command

# Reference images / screenshots dropped in the folder — never part of
# the deployable game (the game is a single self-contained index.html).
*.png
*.jpg
*.jpeg

# Local reference / design source — NOT part of the deployable game.
Sudoku2.0/
Comparison/
index.html.engine-backup
*.engine-backup

# Local reference + archive (organized May 2026) — not part of the deployable game.
_reference/
_archive/

# Local Finder shortcut to the live site — not part of the deployable game.
*.webloc
